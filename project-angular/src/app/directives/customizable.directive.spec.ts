import { CustomizableDirective } from './customizable.directive';

describe('CustomizableDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomizableDirective();
    expect(directive).toBeTruthy();
  });
});
