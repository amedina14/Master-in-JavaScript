'use strict'

// Bucle for: control structure that repeat n time.
var numeros = 0;
var tope = 10;

for (var i = 0; i <= tope; i++) {
    console.log("Vamos por el numero: " + numeros++);

//    debugger;
}
